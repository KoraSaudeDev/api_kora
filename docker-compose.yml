version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3793:3793"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: development
    restart: always
